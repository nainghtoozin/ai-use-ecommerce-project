import { useState, useRef, useEffect } from 'react';import { Sun, Moon, Monitor, ChevronDown, Check } from 'lucide-react';import { useTheme } from '@/Contexts/ThemeContext';const themes = [    { code: 'light', label: 'Light', icon: Sun },    { code: 'dark', label: 'Dark', icon: Moon },    { code: 'system', label: 'System', icon: Monitor },];export default function ThemeSwitcher({ compact = false }) {    const { theme, switchTheme } = useTheme();    const [open, setOpen] = useState(false);    const ref = useRef(null);    const currentTheme = themes.find(t => t.code === theme) || themes[2];    useEffect(() => {        function handleClickOutside(e) {            if (ref.current && !ref.current.contains(e.target)) {                setOpen(false);            }        }        document.addEventListener('mousedown', handleClickOutside);        return () => document.removeEventListener('mousedown', handleClickOutside);    }, []);    if (compact) {        return (            <div className="flex items-center gap-1 p-1 bg-gray-100 dark:bg-gray-800 rounded-lg">                {themes.map((t) => {                    const Icon = t.icon;                    return (                        <button                            key={t.code}                            onClick={() => switchTheme(t.code)}                            className={`p-1.5 rounded-md transition-colors ${                                theme === t.code                                    ? 'bg-white dark:bg-gray-700 shadow-sm text-gray-900 dark:text-white'                                    : 'text-gray-500 dark:text-gray-400 hover:text-gray-700 dark:text-gray-300 dark:hover:text-gray-300'                            }`}                            title={t.label}                        >                            <Icon className="w-4 h-4" />                        </button>                    );                })}            </div>        );    }    return (        <div className="relative" ref={ref}>            <button                onClick={() => setOpen(!open)}                className="flex items-center gap-1.5 px-2.5 py-1.5 text-sm text-gray-600  
        $matches = [regex]::Matches(import { useState, useRef, useEffect } from 'react';import { Sun, Moon, Monitor, ChevronDown, Check } from 'lucide-react';import { useTheme } from '@/Contexts/ThemeContext';const themes = [    { code: 'light', label: 'Light', icon: Sun },    { code: 'dark', label: 'Dark', icon: Moon },    { code: 'system', label: 'System', icon: Monitor },];export default function ThemeSwitcher({ compact = false }) {    const { theme, switchTheme } = useTheme();    const [open, setOpen] = useState(false);    const ref = useRef(null);    const currentTheme = themes.find(t => t.code === theme) || themes[2];    useEffect(() => {        function handleClickOutside(e) {            if (ref.current && !ref.current.contains(e.target)) {                setOpen(false);            }        }        document.addEventListener('mousedown', handleClickOutside);        return () => document.removeEventListener('mousedown', handleClickOutside);    }, []);    if (compact) {        return (            <div className="flex items-center gap-1 p-1 bg-gray-100 dark:bg-gray-800 rounded-lg">                {themes.map((t) => {                    const Icon = t.icon;                    return (                        <button                            key={t.code}                            onClick={() => switchTheme(t.code)}                            className={`p-1.5 rounded-md transition-colors ${                                theme === t.code                                    ? 'bg-white dark:bg-gray-700 shadow-sm text-gray-900 dark:text-white'                                    : 'text-gray-500 dark:text-gray-400 hover:text-gray-700 dark:text-gray-300 dark:hover:text-gray-300'                            }`}                            title={t.label}                        >                            <Icon className="w-4 h-4" />                        </button>                    );                })}            </div>        );    }    return (        <div className="relative" ref={ref}>            <button                onClick={() => setOpen(!open)}                className="flex items-center gap-1.5 px-2.5 py-1.5 text-sm text-gray-600 dark:text-gray-400 dark:text-gray-500 hover:text-gray-900 dark:text-gray-100 dark:hover:text-gray-200 hover:bg-gray-100 dark:bg-gray-800 dark:hover:bg-gray-800 rounded-lg transition-colors"                title="Switch Theme"            >                {(() => {                    const Icon = currentTheme.icon;                    return <Icon className="w-4 h-4" />;                })()}                <span className="hidden sm:inline text-xs font-medium">{currentTheme.label}</span>                <ChevronDown className="w-3 h-3" />            </button>            {open && (                <div className="absolute right-0 mt-1 w-40 bg-white dark:bg-gray-800 rounded-lg shadow-lg border border-gray-200 dark:border-gray-800 
        $matches = [regex]::Matches(import { useState, useRef, useEffect } from 'react';import { Sun, Moon, Monitor, ChevronDown, Check } from 'lucide-react';import { useTheme } from '@/Contexts/ThemeContext';const themes = [    { code: 'light', label: 'Light', icon: Sun },    { code: 'dark', label: 'Dark', icon: Moon },    { code: 'system', label: 'System', icon: Monitor },];export default function ThemeSwitcher({ compact = false }) {    const { theme, switchTheme } = useTheme();    const [open, setOpen] = useState(false);    const ref = useRef(null);    const currentTheme = themes.find(t => t.code === theme) || themes[2];    useEffect(() => {        function handleClickOutside(e) {            if (ref.current && !ref.current.contains(e.target)) {                setOpen(false);            }        }        document.addEventListener('mousedown', handleClickOutside);        return () => document.removeEventListener('mousedown', handleClickOutside);    }, []);    if (compact) {        return (            <div className="flex items-center gap-1 p-1 bg-gray-100 dark:bg-gray-800 rounded-lg">                {themes.map((t) => {                    const Icon = t.icon;                    return (                        <button                            key={t.code}                            onClick={() => switchTheme(t.code)}                            className={`p-1.5 rounded-md transition-colors ${                                theme === t.code                                    ? 'bg-white dark:bg-gray-700 shadow-sm text-gray-900 dark:text-white'                                    : 'text-gray-500 dark:text-gray-400 hover:text-gray-700 dark:text-gray-300 dark:hover:text-gray-300'                            }`}                            title={t.label}                        >                            <Icon className="w-4 h-4" />                        </button>                    );                })}            </div>        );    }    return (        <div className="relative" ref={ref}>            <button                onClick={() => setOpen(!open)}                className="flex items-center gap-1.5 px-2.5 py-1.5 text-sm text-gray-600  
        $matches = [regex]::Matches(import { useState, useRef, useEffect } from 'react';import { Sun, Moon, Monitor, ChevronDown, Check } from 'lucide-react';import { useTheme } from '@/Contexts/ThemeContext';const themes = [    { code: 'light', label: 'Light', icon: Sun },    { code: 'dark', label: 'Dark', icon: Moon },    { code: 'system', label: 'System', icon: Monitor },];export default function ThemeSwitcher({ compact = false }) {    const { theme, switchTheme } = useTheme();    const [open, setOpen] = useState(false);    const ref = useRef(null);    const currentTheme = themes.find(t => t.code === theme) || themes[2];    useEffect(() => {        function handleClickOutside(e) {            if (ref.current && !ref.current.contains(e.target)) {                setOpen(false);            }        }        document.addEventListener('mousedown', handleClickOutside);        return () => document.removeEventListener('mousedown', handleClickOutside);    }, []);    if (compact) {        return (            <div className="flex items-center gap-1 p-1 bg-gray-100 dark:bg-gray-800 rounded-lg">                {themes.map((t) => {                    const Icon = t.icon;                    return (                        <button                            key={t.code}                            onClick={() => switchTheme(t.code)}                            className={`p-1.5 rounded-md transition-colors ${                                theme === t.code                                    ? 'bg-white dark:bg-gray-700 shadow-sm text-gray-900 dark:text-white'                                    : 'text-gray-500 dark:text-gray-400 hover:text-gray-700 dark:text-gray-300 dark:hover:text-gray-300'                            }`}                            title={t.label}                        >                            <Icon className="w-4 h-4" />                        </button>                    );                })}            </div>        );    }    return (        <div className="relative" ref={ref}>            <button                onClick={() => setOpen(!open)}                className="flex items-center gap-1.5 px-2.5 py-1.5 text-sm text-gray-600 dark:text-gray-400 dark:text-gray-500 hover:text-gray-900 dark:text-gray-100 dark:hover:text-gray-200 hover:bg-gray-100 dark:bg-gray-800 dark:hover:bg-gray-800 rounded-lg transition-colors"                title="Switch Theme"            >                {(() => {                    const Icon = currentTheme.icon;                    return <Icon className="w-4 h-4" />;                })()}                <span className="hidden sm:inline text-xs font-medium">{currentTheme.label}</span>                <ChevronDown className="w-3 h-3" />            </button>            {open && (                <div className="absolute right-0 mt-1 w-40 bg-white dark:bg-gray-800 rounded-lg shadow-lg border border-gray-200 dark:border-gray-800 dark:border-gray-700 py-1 z-50">                    {themes.map((t) => {                        const Icon = t.icon;                        return (                            <button                                key={t.code}                                onClick={() => {                                    switchTheme(t.code);                                    setOpen(false);                                }}                                className={`w-full flex items-center gap-2 px-3 py-2 text-sm hover:bg-gray-50 dark:bg-gray-950 dark:hover:bg-gray-700 transition-colors ${                                    theme === t.code ? 'text-blue-600 dark:text-blue-400 font-medium' : 'text-gray-700 dark:text-gray-300'                                }`}                            >                                <Icon className="w-4 h-4" />                                <span className="flex-1">{t.label}</span>                                {theme === t.code && <Check className="w-4 h-4 text-blue-600 dark:text-blue-400" />}                            </button>                        );                    })}                </div>            )}        </div>    );}.Value, 'dark:text-gray-\d+')
        if ($matches.Count -ge 2) { $matches[1].Value } else { import { useState, useRef, useEffect } from 'react';import { Sun, Moon, Monitor, ChevronDown, Check } from 'lucide-react';import { useTheme } from '@/Contexts/ThemeContext';const themes = [    { code: 'light', label: 'Light', icon: Sun },    { code: 'dark', label: 'Dark', icon: Moon },    { code: 'system', label: 'System', icon: Monitor },];export default function ThemeSwitcher({ compact = false }) {    const { theme, switchTheme } = useTheme();    const [open, setOpen] = useState(false);    const ref = useRef(null);    const currentTheme = themes.find(t => t.code === theme) || themes[2];    useEffect(() => {        function handleClickOutside(e) {            if (ref.current && !ref.current.contains(e.target)) {                setOpen(false);            }        }        document.addEventListener('mousedown', handleClickOutside);        return () => document.removeEventListener('mousedown', handleClickOutside);    }, []);    if (compact) {        return (            <div className="flex items-center gap-1 p-1 bg-gray-100 dark:bg-gray-800 rounded-lg">                {themes.map((t) => {                    const Icon = t.icon;                    return (                        <button                            key={t.code}                            onClick={() => switchTheme(t.code)}                            className={`p-1.5 rounded-md transition-colors ${                                theme === t.code                                    ? 'bg-white dark:bg-gray-700 shadow-sm text-gray-900 dark:text-white'                                    : 'text-gray-500 dark:text-gray-400 hover:text-gray-700 dark:text-gray-300 dark:hover:text-gray-300'                            }`}                            title={t.label}                        >                            <Icon className="w-4 h-4" />                        </button>                    );                })}            </div>        );    }    return (        <div className="relative" ref={ref}>            <button                onClick={() => setOpen(!open)}                className="flex items-center gap-1.5 px-2.5 py-1.5 text-sm text-gray-600 dark:text-gray-400 dark:text-gray-500 hover:text-gray-900 dark:text-gray-100 dark:hover:text-gray-200 hover:bg-gray-100 dark:bg-gray-800 dark:hover:bg-gray-800 rounded-lg transition-colors"                title="Switch Theme"            >                {(() => {                    const Icon = currentTheme.icon;                    return <Icon className="w-4 h-4" />;                })()}                <span className="hidden sm:inline text-xs font-medium">{currentTheme.label}</span>                <ChevronDown className="w-3 h-3" />            </button>            {open && (                <div className="absolute right-0 mt-1 w-40 bg-white dark:bg-gray-800 rounded-lg shadow-lg border border-gray-200 dark:border-gray-800 dark:border-gray-700 py-1 z-50">                    {themes.map((t) => {                        const Icon = t.icon;                        return (                            <button                                key={t.code}                                onClick={() => {                                    switchTheme(t.code);                                    setOpen(false);                                }}                                className={`w-full flex items-center gap-2 px-3 py-2 text-sm hover:bg-gray-50 dark:bg-gray-950 dark:hover:bg-gray-700 transition-colors ${                                    theme === t.code ? 'text-blue-600 dark:text-blue-400 font-medium' : 'text-gray-700 dark:text-gray-300'                                }`}                            >                                <Icon className="w-4 h-4" />                                <span className="flex-1">{t.label}</span>                                {theme === t.code && <Check className="w-4 h-4 text-blue-600 dark:text-blue-400" />}                            </button>                        );                    })}                </div>            )}        </div>    );}.Value }
     hover:text-gray-900 dark:text-gray-100 dark:hover:text-gray-200 hover:bg-gray-100 dark:bg-gray-800 dark:hover:bg-gray-800 rounded-lg transition-colors"                title="Switch Theme"            >                {(() => {                    const Icon = currentTheme.icon;                    return <Icon className="w-4 h-4" />;                })()}                <span className="hidden sm:inline text-xs font-medium">{currentTheme.label}</span>                <ChevronDown className="w-3 h-3" />            </button>            {open && (                <div className="absolute right-0 mt-1 w-40 bg-white dark:bg-gray-800 rounded-lg shadow-lg border border-gray-200 dark:border-gray-800 dark:border-gray-700 py-1 z-50">                    {themes.map((t) => {                        const Icon = t.icon;                        return (                            <button                                key={t.code}                                onClick={() => {                                    switchTheme(t.code);                                    setOpen(false);                                }}                                className={`w-full flex items-center gap-2 px-3 py-2 text-sm hover:bg-gray-50 dark:bg-gray-950 dark:hover:bg-gray-700 transition-colors ${                                    theme === t.code ? 'text-blue-600 dark:text-blue-400 font-medium' : 'text-gray-700 dark:text-gray-300'                                }`}                            >                                <Icon className="w-4 h-4" />                                <span className="flex-1">{t.label}</span>                                {theme === t.code && <Check className="w-4 h-4 text-blue-600 dark:text-blue-400" />}                            </button>                        );                    })}                </div>            )}        </div>    );}.Value, 'dark:border-gray-\d+')
        if ($matches.Count -ge 2) { $matches[1].Value } else { import { useState, useRef, useEffect } from 'react';import { Sun, Moon, Monitor, ChevronDown, Check } from 'lucide-react';import { useTheme } from '@/Contexts/ThemeContext';const themes = [    { code: 'light', label: 'Light', icon: Sun },    { code: 'dark', label: 'Dark', icon: Moon },    { code: 'system', label: 'System', icon: Monitor },];export default function ThemeSwitcher({ compact = false }) {    const { theme, switchTheme } = useTheme();    const [open, setOpen] = useState(false);    const ref = useRef(null);    const currentTheme = themes.find(t => t.code === theme) || themes[2];    useEffect(() => {        function handleClickOutside(e) {            if (ref.current && !ref.current.contains(e.target)) {                setOpen(false);            }        }        document.addEventListener('mousedown', handleClickOutside);        return () => document.removeEventListener('mousedown', handleClickOutside);    }, []);    if (compact) {        return (            <div className="flex items-center gap-1 p-1 bg-gray-100 dark:bg-gray-800 rounded-lg">                {themes.map((t) => {                    const Icon = t.icon;                    return (                        <button                            key={t.code}                            onClick={() => switchTheme(t.code)}                            className={`p-1.5 rounded-md transition-colors ${                                theme === t.code                                    ? 'bg-white dark:bg-gray-700 shadow-sm text-gray-900 dark:text-white'                                    : 'text-gray-500 dark:text-gray-400 hover:text-gray-700 dark:text-gray-300 dark:hover:text-gray-300'                            }`}                            title={t.label}                        >                            <Icon className="w-4 h-4" />                        </button>                    );                })}            </div>        );    }    return (        <div className="relative" ref={ref}>            <button                onClick={() => setOpen(!open)}                className="flex items-center gap-1.5 px-2.5 py-1.5 text-sm text-gray-600  
        $matches = [regex]::Matches(import { useState, useRef, useEffect } from 'react';import { Sun, Moon, Monitor, ChevronDown, Check } from 'lucide-react';import { useTheme } from '@/Contexts/ThemeContext';const themes = [    { code: 'light', label: 'Light', icon: Sun },    { code: 'dark', label: 'Dark', icon: Moon },    { code: 'system', label: 'System', icon: Monitor },];export default function ThemeSwitcher({ compact = false }) {    const { theme, switchTheme } = useTheme();    const [open, setOpen] = useState(false);    const ref = useRef(null);    const currentTheme = themes.find(t => t.code === theme) || themes[2];    useEffect(() => {        function handleClickOutside(e) {            if (ref.current && !ref.current.contains(e.target)) {                setOpen(false);            }        }        document.addEventListener('mousedown', handleClickOutside);        return () => document.removeEventListener('mousedown', handleClickOutside);    }, []);    if (compact) {        return (            <div className="flex items-center gap-1 p-1 bg-gray-100 dark:bg-gray-800 rounded-lg">                {themes.map((t) => {                    const Icon = t.icon;                    return (                        <button                            key={t.code}                            onClick={() => switchTheme(t.code)}                            className={`p-1.5 rounded-md transition-colors ${                                theme === t.code                                    ? 'bg-white dark:bg-gray-700 shadow-sm text-gray-900 dark:text-white'                                    : 'text-gray-500 dark:text-gray-400 hover:text-gray-700 dark:text-gray-300 dark:hover:text-gray-300'                            }`}                            title={t.label}                        >                            <Icon className="w-4 h-4" />                        </button>                    );                })}            </div>        );    }    return (        <div className="relative" ref={ref}>            <button                onClick={() => setOpen(!open)}                className="flex items-center gap-1.5 px-2.5 py-1.5 text-sm text-gray-600 dark:text-gray-400 dark:text-gray-500 hover:text-gray-900 dark:text-gray-100 dark:hover:text-gray-200 hover:bg-gray-100 dark:bg-gray-800 dark:hover:bg-gray-800 rounded-lg transition-colors"                title="Switch Theme"            >                {(() => {                    const Icon = currentTheme.icon;                    return <Icon className="w-4 h-4" />;                })()}                <span className="hidden sm:inline text-xs font-medium">{currentTheme.label}</span>                <ChevronDown className="w-3 h-3" />            </button>            {open && (                <div className="absolute right-0 mt-1 w-40 bg-white dark:bg-gray-800 rounded-lg shadow-lg border border-gray-200 dark:border-gray-800 dark:border-gray-700 py-1 z-50">                    {themes.map((t) => {                        const Icon = t.icon;                        return (                            <button                                key={t.code}                                onClick={() => {                                    switchTheme(t.code);                                    setOpen(false);                                }}                                className={`w-full flex items-center gap-2 px-3 py-2 text-sm hover:bg-gray-50 dark:bg-gray-950 dark:hover:bg-gray-700 transition-colors ${                                    theme === t.code ? 'text-blue-600 dark:text-blue-400 font-medium' : 'text-gray-700 dark:text-gray-300'                                }`}                            >                                <Icon className="w-4 h-4" />                                <span className="flex-1">{t.label}</span>                                {theme === t.code && <Check className="w-4 h-4 text-blue-600 dark:text-blue-400" />}                            </button>                        );                    })}                </div>            )}        </div>    );}.Value, 'dark:text-gray-\d+')
        if ($matches.Count -ge 2) { $matches[1].Value } else { import { useState, useRef, useEffect } from 'react';import { Sun, Moon, Monitor, ChevronDown, Check } from 'lucide-react';import { useTheme } from '@/Contexts/ThemeContext';const themes = [    { code: 'light', label: 'Light', icon: Sun },    { code: 'dark', label: 'Dark', icon: Moon },    { code: 'system', label: 'System', icon: Monitor },];export default function ThemeSwitcher({ compact = false }) {    const { theme, switchTheme } = useTheme();    const [open, setOpen] = useState(false);    const ref = useRef(null);    const currentTheme = themes.find(t => t.code === theme) || themes[2];    useEffect(() => {        function handleClickOutside(e) {            if (ref.current && !ref.current.contains(e.target)) {                setOpen(false);            }        }        document.addEventListener('mousedown', handleClickOutside);        return () => document.removeEventListener('mousedown', handleClickOutside);    }, []);    if (compact) {        return (            <div className="flex items-center gap-1 p-1 bg-gray-100 dark:bg-gray-800 rounded-lg">                {themes.map((t) => {                    const Icon = t.icon;                    return (                        <button                            key={t.code}                            onClick={() => switchTheme(t.code)}                            className={`p-1.5 rounded-md transition-colors ${                                theme === t.code                                    ? 'bg-white dark:bg-gray-700 shadow-sm text-gray-900 dark:text-white'                                    : 'text-gray-500 dark:text-gray-400 hover:text-gray-700 dark:text-gray-300 dark:hover:text-gray-300'                            }`}                            title={t.label}                        >                            <Icon className="w-4 h-4" />                        </button>                    );                })}            </div>        );    }    return (        <div className="relative" ref={ref}>            <button                onClick={() => setOpen(!open)}                className="flex items-center gap-1.5 px-2.5 py-1.5 text-sm text-gray-600 dark:text-gray-400 dark:text-gray-500 hover:text-gray-900 dark:text-gray-100 dark:hover:text-gray-200 hover:bg-gray-100 dark:bg-gray-800 dark:hover:bg-gray-800 rounded-lg transition-colors"                title="Switch Theme"            >                {(() => {                    const Icon = currentTheme.icon;                    return <Icon className="w-4 h-4" />;                })()}                <span className="hidden sm:inline text-xs font-medium">{currentTheme.label}</span>                <ChevronDown className="w-3 h-3" />            </button>            {open && (                <div className="absolute right-0 mt-1 w-40 bg-white dark:bg-gray-800 rounded-lg shadow-lg border border-gray-200 dark:border-gray-800 dark:border-gray-700 py-1 z-50">                    {themes.map((t) => {                        const Icon = t.icon;                        return (                            <button                                key={t.code}                                onClick={() => {                                    switchTheme(t.code);                                    setOpen(false);                                }}                                className={`w-full flex items-center gap-2 px-3 py-2 text-sm hover:bg-gray-50 dark:bg-gray-950 dark:hover:bg-gray-700 transition-colors ${                                    theme === t.code ? 'text-blue-600 dark:text-blue-400 font-medium' : 'text-gray-700 dark:text-gray-300'                                }`}                            >                                <Icon className="w-4 h-4" />                                <span className="flex-1">{t.label}</span>                                {theme === t.code && <Check className="w-4 h-4 text-blue-600 dark:text-blue-400" />}                            </button>                        );                    })}                </div>            )}        </div>    );}.Value }
     hover:text-gray-900 dark:text-gray-100 dark:hover:text-gray-200 hover:bg-gray-100 dark:bg-gray-800 dark:hover:bg-gray-800 rounded-lg transition-colors"                title="Switch Theme"            >                {(() => {                    const Icon = currentTheme.icon;                    return <Icon className="w-4 h-4" />;                })()}                <span className="hidden sm:inline text-xs font-medium">{currentTheme.label}</span>                <ChevronDown className="w-3 h-3" />            </button>            {open && (                <div className="absolute right-0 mt-1 w-40 bg-white dark:bg-gray-800 rounded-lg shadow-lg border border-gray-200 dark:border-gray-800 dark:border-gray-700 py-1 z-50">                    {themes.map((t) => {                        const Icon = t.icon;                        return (                            <button                                key={t.code}                                onClick={() => {                                    switchTheme(t.code);                                    setOpen(false);                                }}                                className={`w-full flex items-center gap-2 px-3 py-2 text-sm hover:bg-gray-50 dark:bg-gray-950 dark:hover:bg-gray-700 transition-colors ${                                    theme === t.code ? 'text-blue-600 dark:text-blue-400 font-medium' : 'text-gray-700 dark:text-gray-300'                                }`}                            >                                <Icon className="w-4 h-4" />                                <span className="flex-1">{t.label}</span>                                {theme === t.code && <Check className="w-4 h-4 text-blue-600 dark:text-blue-400" />}                            </button>                        );                    })}                </div>            )}        </div>    );}.Value }
     py-1 z-50">                    {themes.map((t) => {                        const Icon = t.icon;                        return (                            <button                                key={t.code}                                onClick={() => {                                    switchTheme(t.code);                                    setOpen(false);                                }}                                className={`w-full flex items-center gap-2 px-3 py-2 text-sm hover:bg-gray-50 dark:bg-gray-950 dark:hover:bg-gray-700 transition-colors ${                                    theme === t.code ? 'text-blue-600 dark:text-blue-400 font-medium' : 'text-gray-700 dark:text-gray-300'                                }`}                            >                                <Icon className="w-4 h-4" />                                <span className="flex-1">{t.label}</span>                                {theme === t.code && <Check className="w-4 h-4 text-blue-600 dark:text-blue-400" />}                            </button>                        );                    })}                </div>            )}        </div>    );}.Value, 'dark:text-gray-\d+')
        if ($matches.Count -ge 2) { $matches[1].Value } else { import { useState, useRef, useEffect } from 'react';import { Sun, Moon, Monitor, ChevronDown, Check } from 'lucide-react';import { useTheme } from '@/Contexts/ThemeContext';const themes = [    { code: 'light', label: 'Light', icon: Sun },    { code: 'dark', label: 'Dark', icon: Moon },    { code: 'system', label: 'System', icon: Monitor },];export default function ThemeSwitcher({ compact = false }) {    const { theme, switchTheme } = useTheme();    const [open, setOpen] = useState(false);    const ref = useRef(null);    const currentTheme = themes.find(t => t.code === theme) || themes[2];    useEffect(() => {        function handleClickOutside(e) {            if (ref.current && !ref.current.contains(e.target)) {                setOpen(false);            }        }        document.addEventListener('mousedown', handleClickOutside);        return () => document.removeEventListener('mousedown', handleClickOutside);    }, []);    if (compact) {        return (            <div className="flex items-center gap-1 p-1 bg-gray-100 dark:bg-gray-800 rounded-lg">                {themes.map((t) => {                    const Icon = t.icon;                    return (                        <button                            key={t.code}                            onClick={() => switchTheme(t.code)}                            className={`p-1.5 rounded-md transition-colors ${                                theme === t.code                                    ? 'bg-white dark:bg-gray-700 shadow-sm text-gray-900 dark:text-white'                                    : 'text-gray-500 dark:text-gray-400 hover:text-gray-700 dark:text-gray-300 dark:hover:text-gray-300'                            }`}                            title={t.label}                        >                            <Icon className="w-4 h-4" />                        </button>                    );                })}            </div>        );    }    return (        <div className="relative" ref={ref}>            <button                onClick={() => setOpen(!open)}                className="flex items-center gap-1.5 px-2.5 py-1.5 text-sm text-gray-600 dark:text-gray-400 dark:text-gray-500 hover:text-gray-900 dark:text-gray-100 dark:hover:text-gray-200 hover:bg-gray-100 dark:bg-gray-800 dark:hover:bg-gray-800 rounded-lg transition-colors"                title="Switch Theme"            >                {(() => {                    const Icon = currentTheme.icon;                    return <Icon className="w-4 h-4" />;                })()}                <span className="hidden sm:inline text-xs font-medium">{currentTheme.label}</span>                <ChevronDown className="w-3 h-3" />            </button>            {open && (                <div className="absolute right-0 mt-1 w-40 bg-white dark:bg-gray-800 rounded-lg shadow-lg border border-gray-200 dark:border-gray-800 
        $matches = [regex]::Matches(import { useState, useRef, useEffect } from 'react';import { Sun, Moon, Monitor, ChevronDown, Check } from 'lucide-react';import { useTheme } from '@/Contexts/ThemeContext';const themes = [    { code: 'light', label: 'Light', icon: Sun },    { code: 'dark', label: 'Dark', icon: Moon },    { code: 'system', label: 'System', icon: Monitor },];export default function ThemeSwitcher({ compact = false }) {    const { theme, switchTheme } = useTheme();    const [open, setOpen] = useState(false);    const ref = useRef(null);    const currentTheme = themes.find(t => t.code === theme) || themes[2];    useEffect(() => {        function handleClickOutside(e) {            if (ref.current && !ref.current.contains(e.target)) {                setOpen(false);            }        }        document.addEventListener('mousedown', handleClickOutside);        return () => document.removeEventListener('mousedown', handleClickOutside);    }, []);    if (compact) {        return (            <div className="flex items-center gap-1 p-1 bg-gray-100 dark:bg-gray-800 rounded-lg">                {themes.map((t) => {                    const Icon = t.icon;                    return (                        <button                            key={t.code}                            onClick={() => switchTheme(t.code)}                            className={`p-1.5 rounded-md transition-colors ${                                theme === t.code                                    ? 'bg-white dark:bg-gray-700 shadow-sm text-gray-900 dark:text-white'                                    : 'text-gray-500 dark:text-gray-400 hover:text-gray-700 dark:text-gray-300 dark:hover:text-gray-300'                            }`}                            title={t.label}                        >                            <Icon className="w-4 h-4" />                        </button>                    );                })}            </div>        );    }    return (        <div className="relative" ref={ref}>            <button                onClick={() => setOpen(!open)}                className="flex items-center gap-1.5 px-2.5 py-1.5 text-sm text-gray-600  
        $matches = [regex]::Matches(import { useState, useRef, useEffect } from 'react';import { Sun, Moon, Monitor, ChevronDown, Check } from 'lucide-react';import { useTheme } from '@/Contexts/ThemeContext';const themes = [    { code: 'light', label: 'Light', icon: Sun },    { code: 'dark', label: 'Dark', icon: Moon },    { code: 'system', label: 'System', icon: Monitor },];export default function ThemeSwitcher({ compact = false }) {    const { theme, switchTheme } = useTheme();    const [open, setOpen] = useState(false);    const ref = useRef(null);    const currentTheme = themes.find(t => t.code === theme) || themes[2];    useEffect(() => {        function handleClickOutside(e) {            if (ref.current && !ref.current.contains(e.target)) {                setOpen(false);            }        }        document.addEventListener('mousedown', handleClickOutside);        return () => document.removeEventListener('mousedown', handleClickOutside);    }, []);    if (compact) {        return (            <div className="flex items-center gap-1 p-1 bg-gray-100 dark:bg-gray-800 rounded-lg">                {themes.map((t) => {                    const Icon = t.icon;                    return (                        <button                            key={t.code}                            onClick={() => switchTheme(t.code)}                            className={`p-1.5 rounded-md transition-colors ${                                theme === t.code                                    ? 'bg-white dark:bg-gray-700 shadow-sm text-gray-900 dark:text-white'                                    : 'text-gray-500 dark:text-gray-400 hover:text-gray-700 dark:text-gray-300 dark:hover:text-gray-300'                            }`}                            title={t.label}                        >                            <Icon className="w-4 h-4" />                        </button>                    );                })}            </div>        );    }    return (        <div className="relative" ref={ref}>            <button                onClick={() => setOpen(!open)}                className="flex items-center gap-1.5 px-2.5 py-1.5 text-sm text-gray-600 dark:text-gray-400 dark:text-gray-500 hover:text-gray-900 dark:text-gray-100 dark:hover:text-gray-200 hover:bg-gray-100 dark:bg-gray-800 dark:hover:bg-gray-800 rounded-lg transition-colors"                title="Switch Theme"            >                {(() => {                    const Icon = currentTheme.icon;                    return <Icon className="w-4 h-4" />;                })()}                <span className="hidden sm:inline text-xs font-medium">{currentTheme.label}</span>                <ChevronDown className="w-3 h-3" />            </button>            {open && (                <div className="absolute right-0 mt-1 w-40 bg-white dark:bg-gray-800 rounded-lg shadow-lg border border-gray-200 dark:border-gray-800 dark:border-gray-700 py-1 z-50">                    {themes.map((t) => {                        const Icon = t.icon;                        return (                            <button                                key={t.code}                                onClick={() => {                                    switchTheme(t.code);                                    setOpen(false);                                }}                                className={`w-full flex items-center gap-2 px-3 py-2 text-sm hover:bg-gray-50 dark:bg-gray-950 dark:hover:bg-gray-700 transition-colors ${                                    theme === t.code ? 'text-blue-600 dark:text-blue-400 font-medium' : 'text-gray-700 dark:text-gray-300'                                }`}                            >                                <Icon className="w-4 h-4" />                                <span className="flex-1">{t.label}</span>                                {theme === t.code && <Check className="w-4 h-4 text-blue-600 dark:text-blue-400" />}                            </button>                        );                    })}                </div>            )}        </div>    );}.Value, 'dark:text-gray-\d+')
        if ($matches.Count -ge 2) { $matches[1].Value } else { import { useState, useRef, useEffect } from 'react';import { Sun, Moon, Monitor, ChevronDown, Check } from 'lucide-react';import { useTheme } from '@/Contexts/ThemeContext';const themes = [    { code: 'light', label: 'Light', icon: Sun },    { code: 'dark', label: 'Dark', icon: Moon },    { code: 'system', label: 'System', icon: Monitor },];export default function ThemeSwitcher({ compact = false }) {    const { theme, switchTheme } = useTheme();    const [open, setOpen] = useState(false);    const ref = useRef(null);    const currentTheme = themes.find(t => t.code === theme) || themes[2];    useEffect(() => {        function handleClickOutside(e) {            if (ref.current && !ref.current.contains(e.target)) {                setOpen(false);            }        }        document.addEventListener('mousedown', handleClickOutside);        return () => document.removeEventListener('mousedown', handleClickOutside);    }, []);    if (compact) {        return (            <div className="flex items-center gap-1 p-1 bg-gray-100 dark:bg-gray-800 rounded-lg">                {themes.map((t) => {                    const Icon = t.icon;                    return (                        <button                            key={t.code}                            onClick={() => switchTheme(t.code)}                            className={`p-1.5 rounded-md transition-colors ${                                theme === t.code                                    ? 'bg-white dark:bg-gray-700 shadow-sm text-gray-900 dark:text-white'                                    : 'text-gray-500 dark:text-gray-400 hover:text-gray-700 dark:text-gray-300 dark:hover:text-gray-300'                            }`}                            title={t.label}                        >                            <Icon className="w-4 h-4" />                        </button>                    );                })}            </div>        );    }    return (        <div className="relative" ref={ref}>            <button                onClick={() => setOpen(!open)}                className="flex items-center gap-1.5 px-2.5 py-1.5 text-sm text-gray-600 dark:text-gray-400 dark:text-gray-500 hover:text-gray-900 dark:text-gray-100 dark:hover:text-gray-200 hover:bg-gray-100 dark:bg-gray-800 dark:hover:bg-gray-800 rounded-lg transition-colors"                title="Switch Theme"            >                {(() => {                    const Icon = currentTheme.icon;                    return <Icon className="w-4 h-4" />;                })()}                <span className="hidden sm:inline text-xs font-medium">{currentTheme.label}</span>                <ChevronDown className="w-3 h-3" />            </button>            {open && (                <div className="absolute right-0 mt-1 w-40 bg-white dark:bg-gray-800 rounded-lg shadow-lg border border-gray-200 dark:border-gray-800 dark:border-gray-700 py-1 z-50">                    {themes.map((t) => {                        const Icon = t.icon;                        return (                            <button                                key={t.code}                                onClick={() => {                                    switchTheme(t.code);                                    setOpen(false);                                }}                                className={`w-full flex items-center gap-2 px-3 py-2 text-sm hover:bg-gray-50 dark:bg-gray-950 dark:hover:bg-gray-700 transition-colors ${                                    theme === t.code ? 'text-blue-600 dark:text-blue-400 font-medium' : 'text-gray-700 dark:text-gray-300'                                }`}                            >                                <Icon className="w-4 h-4" />                                <span className="flex-1">{t.label}</span>                                {theme === t.code && <Check className="w-4 h-4 text-blue-600 dark:text-blue-400" />}                            </button>                        );                    })}                </div>            )}        </div>    );}.Value }
     hover:text-gray-900 dark:text-gray-100 dark:hover:text-gray-200 hover:bg-gray-100 dark:bg-gray-800 dark:hover:bg-gray-800 rounded-lg transition-colors"                title="Switch Theme"            >                {(() => {                    const Icon = currentTheme.icon;                    return <Icon className="w-4 h-4" />;                })()}                <span className="hidden sm:inline text-xs font-medium">{currentTheme.label}</span>                <ChevronDown className="w-3 h-3" />            </button>            {open && (                <div className="absolute right-0 mt-1 w-40 bg-white dark:bg-gray-800 rounded-lg shadow-lg border border-gray-200 dark:border-gray-800 dark:border-gray-700 py-1 z-50">                    {themes.map((t) => {                        const Icon = t.icon;                        return (                            <button                                key={t.code}                                onClick={() => {                                    switchTheme(t.code);                                    setOpen(false);                                }}                                className={`w-full flex items-center gap-2 px-3 py-2 text-sm hover:bg-gray-50 dark:bg-gray-950 dark:hover:bg-gray-700 transition-colors ${                                    theme === t.code ? 'text-blue-600 dark:text-blue-400 font-medium' : 'text-gray-700 dark:text-gray-300'                                }`}                            >                                <Icon className="w-4 h-4" />                                <span className="flex-1">{t.label}</span>                                {theme === t.code && <Check className="w-4 h-4 text-blue-600 dark:text-blue-400" />}                            </button>                        );                    })}                </div>            )}        </div>    );}.Value, 'dark:border-gray-\d+')
        if ($matches.Count -ge 2) { $matches[1].Value } else { import { useState, useRef, useEffect } from 'react';import { Sun, Moon, Monitor, ChevronDown, Check } from 'lucide-react';import { useTheme } from '@/Contexts/ThemeContext';const themes = [    { code: 'light', label: 'Light', icon: Sun },    { code: 'dark', label: 'Dark', icon: Moon },    { code: 'system', label: 'System', icon: Monitor },];export default function ThemeSwitcher({ compact = false }) {    const { theme, switchTheme } = useTheme();    const [open, setOpen] = useState(false);    const ref = useRef(null);    const currentTheme = themes.find(t => t.code === theme) || themes[2];    useEffect(() => {        function handleClickOutside(e) {            if (ref.current && !ref.current.contains(e.target)) {                setOpen(false);            }        }        document.addEventListener('mousedown', handleClickOutside);        return () => document.removeEventListener('mousedown', handleClickOutside);    }, []);    if (compact) {        return (            <div className="flex items-center gap-1 p-1 bg-gray-100 dark:bg-gray-800 rounded-lg">                {themes.map((t) => {                    const Icon = t.icon;                    return (                        <button                            key={t.code}                            onClick={() => switchTheme(t.code)}                            className={`p-1.5 rounded-md transition-colors ${                                theme === t.code                                    ? 'bg-white dark:bg-gray-700 shadow-sm text-gray-900 dark:text-white'                                    : 'text-gray-500 dark:text-gray-400 hover:text-gray-700 dark:text-gray-300 dark:hover:text-gray-300'                            }`}                            title={t.label}                        >                            <Icon className="w-4 h-4" />                        </button>                    );                })}            </div>        );    }    return (        <div className="relative" ref={ref}>            <button                onClick={() => setOpen(!open)}                className="flex items-center gap-1.5 px-2.5 py-1.5 text-sm text-gray-600  
        $matches = [regex]::Matches(import { useState, useRef, useEffect } from 'react';import { Sun, Moon, Monitor, ChevronDown, Check } from 'lucide-react';import { useTheme } from '@/Contexts/ThemeContext';const themes = [    { code: 'light', label: 'Light', icon: Sun },    { code: 'dark', label: 'Dark', icon: Moon },    { code: 'system', label: 'System', icon: Monitor },];export default function ThemeSwitcher({ compact = false }) {    const { theme, switchTheme } = useTheme();    const [open, setOpen] = useState(false);    const ref = useRef(null);    const currentTheme = themes.find(t => t.code === theme) || themes[2];    useEffect(() => {        function handleClickOutside(e) {            if (ref.current && !ref.current.contains(e.target)) {                setOpen(false);            }        }        document.addEventListener('mousedown', handleClickOutside);        return () => document.removeEventListener('mousedown', handleClickOutside);    }, []);    if (compact) {        return (            <div className="flex items-center gap-1 p-1 bg-gray-100 dark:bg-gray-800 rounded-lg">                {themes.map((t) => {                    const Icon = t.icon;                    return (                        <button                            key={t.code}                            onClick={() => switchTheme(t.code)}                            className={`p-1.5 rounded-md transition-colors ${                                theme === t.code                                    ? 'bg-white dark:bg-gray-700 shadow-sm text-gray-900 dark:text-white'                                    : 'text-gray-500 dark:text-gray-400 hover:text-gray-700 dark:text-gray-300 dark:hover:text-gray-300'                            }`}                            title={t.label}                        >                            <Icon className="w-4 h-4" />                        </button>                    );                })}            </div>        );    }    return (        <div className="relative" ref={ref}>            <button                onClick={() => setOpen(!open)}                className="flex items-center gap-1.5 px-2.5 py-1.5 text-sm text-gray-600 dark:text-gray-400 dark:text-gray-500 hover:text-gray-900 dark:text-gray-100 dark:hover:text-gray-200 hover:bg-gray-100 dark:bg-gray-800 dark:hover:bg-gray-800 rounded-lg transition-colors"                title="Switch Theme"            >                {(() => {                    const Icon = currentTheme.icon;                    return <Icon className="w-4 h-4" />;                })()}                <span className="hidden sm:inline text-xs font-medium">{currentTheme.label}</span>                <ChevronDown className="w-3 h-3" />            </button>            {open && (                <div className="absolute right-0 mt-1 w-40 bg-white dark:bg-gray-800 rounded-lg shadow-lg border border-gray-200 dark:border-gray-800 dark:border-gray-700 py-1 z-50">                    {themes.map((t) => {                        const Icon = t.icon;                        return (                            <button                                key={t.code}                                onClick={() => {                                    switchTheme(t.code);                                    setOpen(false);                                }}                                className={`w-full flex items-center gap-2 px-3 py-2 text-sm hover:bg-gray-50 dark:bg-gray-950 dark:hover:bg-gray-700 transition-colors ${                                    theme === t.code ? 'text-blue-600 dark:text-blue-400 font-medium' : 'text-gray-700 dark:text-gray-300'                                }`}                            >                                <Icon className="w-4 h-4" />                                <span className="flex-1">{t.label}</span>                                {theme === t.code && <Check className="w-4 h-4 text-blue-600 dark:text-blue-400" />}                            </button>                        );                    })}                </div>            )}        </div>    );}.Value, 'dark:text-gray-\d+')
        if ($matches.Count -ge 2) { $matches[1].Value } else { import { useState, useRef, useEffect } from 'react';import { Sun, Moon, Monitor, ChevronDown, Check } from 'lucide-react';import { useTheme } from '@/Contexts/ThemeContext';const themes = [    { code: 'light', label: 'Light', icon: Sun },    { code: 'dark', label: 'Dark', icon: Moon },    { code: 'system', label: 'System', icon: Monitor },];export default function ThemeSwitcher({ compact = false }) {    const { theme, switchTheme } = useTheme();    const [open, setOpen] = useState(false);    const ref = useRef(null);    const currentTheme = themes.find(t => t.code === theme) || themes[2];    useEffect(() => {        function handleClickOutside(e) {            if (ref.current && !ref.current.contains(e.target)) {                setOpen(false);            }        }        document.addEventListener('mousedown', handleClickOutside);        return () => document.removeEventListener('mousedown', handleClickOutside);    }, []);    if (compact) {        return (            <div className="flex items-center gap-1 p-1 bg-gray-100 dark:bg-gray-800 rounded-lg">                {themes.map((t) => {                    const Icon = t.icon;                    return (                        <button                            key={t.code}                            onClick={() => switchTheme(t.code)}                            className={`p-1.5 rounded-md transition-colors ${                                theme === t.code                                    ? 'bg-white dark:bg-gray-700 shadow-sm text-gray-900 dark:text-white'                                    : 'text-gray-500 dark:text-gray-400 hover:text-gray-700 dark:text-gray-300 dark:hover:text-gray-300'                            }`}                            title={t.label}                        >                            <Icon className="w-4 h-4" />                        </button>                    );                })}            </div>        );    }    return (        <div className="relative" ref={ref}>            <button                onClick={() => setOpen(!open)}                className="flex items-center gap-1.5 px-2.5 py-1.5 text-sm text-gray-600 dark:text-gray-400 dark:text-gray-500 hover:text-gray-900 dark:text-gray-100 dark:hover:text-gray-200 hover:bg-gray-100 dark:bg-gray-800 dark:hover:bg-gray-800 rounded-lg transition-colors"                title="Switch Theme"            >                {(() => {                    const Icon = currentTheme.icon;                    return <Icon className="w-4 h-4" />;                })()}                <span className="hidden sm:inline text-xs font-medium">{currentTheme.label}</span>                <ChevronDown className="w-3 h-3" />            </button>            {open && (                <div className="absolute right-0 mt-1 w-40 bg-white dark:bg-gray-800 rounded-lg shadow-lg border border-gray-200 dark:border-gray-800 dark:border-gray-700 py-1 z-50">                    {themes.map((t) => {                        const Icon = t.icon;                        return (                            <button                                key={t.code}                                onClick={() => {                                    switchTheme(t.code);                                    setOpen(false);                                }}                                className={`w-full flex items-center gap-2 px-3 py-2 text-sm hover:bg-gray-50 dark:bg-gray-950 dark:hover:bg-gray-700 transition-colors ${                                    theme === t.code ? 'text-blue-600 dark:text-blue-400 font-medium' : 'text-gray-700 dark:text-gray-300'                                }`}                            >                                <Icon className="w-4 h-4" />                                <span className="flex-1">{t.label}</span>                                {theme === t.code && <Check className="w-4 h-4 text-blue-600 dark:text-blue-400" />}                            </button>                        );                    })}                </div>            )}        </div>    );}.Value }
     hover:text-gray-900 dark:text-gray-100 dark:hover:text-gray-200 hover:bg-gray-100 dark:bg-gray-800 dark:hover:bg-gray-800 rounded-lg transition-colors"                title="Switch Theme"            >                {(() => {                    const Icon = currentTheme.icon;                    return <Icon className="w-4 h-4" />;                })()}                <span className="hidden sm:inline text-xs font-medium">{currentTheme.label}</span>                <ChevronDown className="w-3 h-3" />            </button>            {open && (                <div className="absolute right-0 mt-1 w-40 bg-white dark:bg-gray-800 rounded-lg shadow-lg border border-gray-200 dark:border-gray-800 dark:border-gray-700 py-1 z-50">                    {themes.map((t) => {                        const Icon = t.icon;                        return (                            <button                                key={t.code}                                onClick={() => {                                    switchTheme(t.code);                                    setOpen(false);                                }}                                className={`w-full flex items-center gap-2 px-3 py-2 text-sm hover:bg-gray-50 dark:bg-gray-950 dark:hover:bg-gray-700 transition-colors ${                                    theme === t.code ? 'text-blue-600 dark:text-blue-400 font-medium' : 'text-gray-700 dark:text-gray-300'                                }`}                            >                                <Icon className="w-4 h-4" />                                <span className="flex-1">{t.label}</span>                                {theme === t.code && <Check className="w-4 h-4 text-blue-600 dark:text-blue-400" />}                            </button>                        );                    })}                </div>            )}        </div>    );}.Value }
     py-1 z-50">                    {themes.map((t) => {                        const Icon = t.icon;                        return (                            <button                                key={t.code}                                onClick={() => {                                    switchTheme(t.code);                                    setOpen(false);                                }}                                className={`w-full flex items-center gap-2 px-3 py-2 text-sm hover:bg-gray-50 dark:bg-gray-950 dark:hover:bg-gray-700 transition-colors ${                                    theme === t.code ? 'text-blue-600 dark:text-blue-400 font-medium' : 'text-gray-700 dark:text-gray-300'                                }`}                            >                                <Icon className="w-4 h-4" />                                <span className="flex-1">{t.label}</span>                                {theme === t.code && <Check className="w-4 h-4 text-blue-600 dark:text-blue-400" />}                            </button>                        );                    })}                </div>            )}        </div>    );}.Value }
     hover:text-gray-900 dark:text-gray-100 dark:hover:text-gray-200 hover:bg-gray-100 dark:bg-gray-800 dark:hover:bg-gray-800 rounded-lg transition-colors"                title="Switch Theme"            >                {(() => {                    const Icon = currentTheme.icon;                    return <Icon className="w-4 h-4" />;                })()}                <span className="hidden sm:inline text-xs font-medium">{currentTheme.label}</span>                <ChevronDown className="w-3 h-3" />            </button>            {open && (                <div className="absolute right-0 mt-1 w-40 bg-white dark:bg-gray-800 rounded-lg shadow-lg border border-gray-200 dark:border-gray-800 
        $matches = [regex]::Matches(import { useState, useRef, useEffect } from 'react';import { Sun, Moon, Monitor, ChevronDown, Check } from 'lucide-react';import { useTheme } from '@/Contexts/ThemeContext';const themes = [    { code: 'light', label: 'Light', icon: Sun },    { code: 'dark', label: 'Dark', icon: Moon },    { code: 'system', label: 'System', icon: Monitor },];export default function ThemeSwitcher({ compact = false }) {    const { theme, switchTheme } = useTheme();    const [open, setOpen] = useState(false);    const ref = useRef(null);    const currentTheme = themes.find(t => t.code === theme) || themes[2];    useEffect(() => {        function handleClickOutside(e) {            if (ref.current && !ref.current.contains(e.target)) {                setOpen(false);            }        }        document.addEventListener('mousedown', handleClickOutside);        return () => document.removeEventListener('mousedown', handleClickOutside);    }, []);    if (compact) {        return (            <div className="flex items-center gap-1 p-1 bg-gray-100 dark:bg-gray-800 rounded-lg">                {themes.map((t) => {                    const Icon = t.icon;                    return (                        <button                            key={t.code}                            onClick={() => switchTheme(t.code)}                            className={`p-1.5 rounded-md transition-colors ${                                theme === t.code                                    ? 'bg-white dark:bg-gray-700 shadow-sm text-gray-900 dark:text-white'                                    : 'text-gray-500 dark:text-gray-400 hover:text-gray-700 dark:text-gray-300 dark:hover:text-gray-300'                            }`}                            title={t.label}                        >                            <Icon className="w-4 h-4" />                        </button>                    );                })}            </div>        );    }    return (        <div className="relative" ref={ref}>            <button                onClick={() => setOpen(!open)}                className="flex items-center gap-1.5 px-2.5 py-1.5 text-sm text-gray-600  
        $matches = [regex]::Matches(import { useState, useRef, useEffect } from 'react';import { Sun, Moon, Monitor, ChevronDown, Check } from 'lucide-react';import { useTheme } from '@/Contexts/ThemeContext';const themes = [    { code: 'light', label: 'Light', icon: Sun },    { code: 'dark', label: 'Dark', icon: Moon },    { code: 'system', label: 'System', icon: Monitor },];export default function ThemeSwitcher({ compact = false }) {    const { theme, switchTheme } = useTheme();    const [open, setOpen] = useState(false);    const ref = useRef(null);    const currentTheme = themes.find(t => t.code === theme) || themes[2];    useEffect(() => {        function handleClickOutside(e) {            if (ref.current && !ref.current.contains(e.target)) {                setOpen(false);            }        }        document.addEventListener('mousedown', handleClickOutside);        return () => document.removeEventListener('mousedown', handleClickOutside);    }, []);    if (compact) {        return (            <div className="flex items-center gap-1 p-1 bg-gray-100 dark:bg-gray-800 rounded-lg">                {themes.map((t) => {                    const Icon = t.icon;                    return (                        <button                            key={t.code}                            onClick={() => switchTheme(t.code)}                            className={`p-1.5 rounded-md transition-colors ${                                theme === t.code                                    ? 'bg-white dark:bg-gray-700 shadow-sm text-gray-900 dark:text-white'                                    : 'text-gray-500 dark:text-gray-400 hover:text-gray-700 dark:text-gray-300 dark:hover:text-gray-300'                            }`}                            title={t.label}                        >                            <Icon className="w-4 h-4" />                        </button>                    );                })}            </div>        );    }    return (        <div className="relative" ref={ref}>            <button                onClick={() => setOpen(!open)}                className="flex items-center gap-1.5 px-2.5 py-1.5 text-sm text-gray-600 dark:text-gray-400 dark:text-gray-500 hover:text-gray-900 dark:text-gray-100 dark:hover:text-gray-200 hover:bg-gray-100 dark:bg-gray-800 dark:hover:bg-gray-800 rounded-lg transition-colors"                title="Switch Theme"            >                {(() => {                    const Icon = currentTheme.icon;                    return <Icon className="w-4 h-4" />;                })()}                <span className="hidden sm:inline text-xs font-medium">{currentTheme.label}</span>                <ChevronDown className="w-3 h-3" />            </button>            {open && (                <div className="absolute right-0 mt-1 w-40 bg-white dark:bg-gray-800 rounded-lg shadow-lg border border-gray-200 dark:border-gray-800 dark:border-gray-700 py-1 z-50">                    {themes.map((t) => {                        const Icon = t.icon;                        return (                            <button                                key={t.code}                                onClick={() => {                                    switchTheme(t.code);                                    setOpen(false);                                }}                                className={`w-full flex items-center gap-2 px-3 py-2 text-sm hover:bg-gray-50 dark:bg-gray-950 dark:hover:bg-gray-700 transition-colors ${                                    theme === t.code ? 'text-blue-600 dark:text-blue-400 font-medium' : 'text-gray-700 dark:text-gray-300'                                }`}                            >                                <Icon className="w-4 h-4" />                                <span className="flex-1">{t.label}</span>                                {theme === t.code && <Check className="w-4 h-4 text-blue-600 dark:text-blue-400" />}                            </button>                        );                    })}                </div>            )}        </div>    );}.Value, 'dark:text-gray-\d+')
        if ($matches.Count -ge 2) { $matches[1].Value } else { import { useState, useRef, useEffect } from 'react';import { Sun, Moon, Monitor, ChevronDown, Check } from 'lucide-react';import { useTheme } from '@/Contexts/ThemeContext';const themes = [    { code: 'light', label: 'Light', icon: Sun },    { code: 'dark', label: 'Dark', icon: Moon },    { code: 'system', label: 'System', icon: Monitor },];export default function ThemeSwitcher({ compact = false }) {    const { theme, switchTheme } = useTheme();    const [open, setOpen] = useState(false);    const ref = useRef(null);    const currentTheme = themes.find(t => t.code === theme) || themes[2];    useEffect(() => {        function handleClickOutside(e) {            if (ref.current && !ref.current.contains(e.target)) {                setOpen(false);            }        }        document.addEventListener('mousedown', handleClickOutside);        return () => document.removeEventListener('mousedown', handleClickOutside);    }, []);    if (compact) {        return (            <div className="flex items-center gap-1 p-1 bg-gray-100 dark:bg-gray-800 rounded-lg">                {themes.map((t) => {                    const Icon = t.icon;                    return (                        <button                            key={t.code}                            onClick={() => switchTheme(t.code)}                            className={`p-1.5 rounded-md transition-colors ${                                theme === t.code                                    ? 'bg-white dark:bg-gray-700 shadow-sm text-gray-900 dark:text-white'                                    : 'text-gray-500 dark:text-gray-400 hover:text-gray-700 dark:text-gray-300 dark:hover:text-gray-300'                            }`}                            title={t.label}                        >                            <Icon className="w-4 h-4" />                        </button>                    );                })}            </div>        );    }    return (        <div className="relative" ref={ref}>            <button                onClick={() => setOpen(!open)}                className="flex items-center gap-1.5 px-2.5 py-1.5 text-sm text-gray-600 dark:text-gray-400 dark:text-gray-500 hover:text-gray-900 dark:text-gray-100 dark:hover:text-gray-200 hover:bg-gray-100 dark:bg-gray-800 dark:hover:bg-gray-800 rounded-lg transition-colors"                title="Switch Theme"            >                {(() => {                    const Icon = currentTheme.icon;                    return <Icon className="w-4 h-4" />;                })()}                <span className="hidden sm:inline text-xs font-medium">{currentTheme.label}</span>                <ChevronDown className="w-3 h-3" />            </button>            {open && (                <div className="absolute right-0 mt-1 w-40 bg-white dark:bg-gray-800 rounded-lg shadow-lg border border-gray-200 dark:border-gray-800 dark:border-gray-700 py-1 z-50">                    {themes.map((t) => {                        const Icon = t.icon;                        return (                            <button                                key={t.code}                                onClick={() => {                                    switchTheme(t.code);                                    setOpen(false);                                }}                                className={`w-full flex items-center gap-2 px-3 py-2 text-sm hover:bg-gray-50 dark:bg-gray-950 dark:hover:bg-gray-700 transition-colors ${                                    theme === t.code ? 'text-blue-600 dark:text-blue-400 font-medium' : 'text-gray-700 dark:text-gray-300'                                }`}                            >                                <Icon className="w-4 h-4" />                                <span className="flex-1">{t.label}</span>                                {theme === t.code && <Check className="w-4 h-4 text-blue-600 dark:text-blue-400" />}                            </button>                        );                    })}                </div>            )}        </div>    );}.Value }
     hover:text-gray-900 dark:text-gray-100 dark:hover:text-gray-200 hover:bg-gray-100 dark:bg-gray-800 dark:hover:bg-gray-800 rounded-lg transition-colors"                title="Switch Theme"            >                {(() => {                    const Icon = currentTheme.icon;                    return <Icon className="w-4 h-4" />;                })()}                <span className="hidden sm:inline text-xs font-medium">{currentTheme.label}</span>                <ChevronDown className="w-3 h-3" />            </button>            {open && (                <div className="absolute right-0 mt-1 w-40 bg-white dark:bg-gray-800 rounded-lg shadow-lg border border-gray-200 dark:border-gray-800 dark:border-gray-700 py-1 z-50">                    {themes.map((t) => {                        const Icon = t.icon;                        return (                            <button                                key={t.code}                                onClick={() => {                                    switchTheme(t.code);                                    setOpen(false);                                }}                                className={`w-full flex items-center gap-2 px-3 py-2 text-sm hover:bg-gray-50 dark:bg-gray-950 dark:hover:bg-gray-700 transition-colors ${                                    theme === t.code ? 'text-blue-600 dark:text-blue-400 font-medium' : 'text-gray-700 dark:text-gray-300'                                }`}                            >                                <Icon className="w-4 h-4" />                                <span className="flex-1">{t.label}</span>                                {theme === t.code && <Check className="w-4 h-4 text-blue-600 dark:text-blue-400" />}                            </button>                        );                    })}                </div>            )}        </div>    );}.Value, 'dark:border-gray-\d+')
        if ($matches.Count -ge 2) { $matches[1].Value } else { import { useState, useRef, useEffect } from 'react';import { Sun, Moon, Monitor, ChevronDown, Check } from 'lucide-react';import { useTheme } from '@/Contexts/ThemeContext';const themes = [    { code: 'light', label: 'Light', icon: Sun },    { code: 'dark', label: 'Dark', icon: Moon },    { code: 'system', label: 'System', icon: Monitor },];export default function ThemeSwitcher({ compact = false }) {    const { theme, switchTheme } = useTheme();    const [open, setOpen] = useState(false);    const ref = useRef(null);    const currentTheme = themes.find(t => t.code === theme) || themes[2];    useEffect(() => {        function handleClickOutside(e) {            if (ref.current && !ref.current.contains(e.target)) {                setOpen(false);            }        }        document.addEventListener('mousedown', handleClickOutside);        return () => document.removeEventListener('mousedown', handleClickOutside);    }, []);    if (compact) {        return (            <div className="flex items-center gap-1 p-1 bg-gray-100 dark:bg-gray-800 rounded-lg">                {themes.map((t) => {                    const Icon = t.icon;                    return (                        <button                            key={t.code}                            onClick={() => switchTheme(t.code)}                            className={`p-1.5 rounded-md transition-colors ${                                theme === t.code                                    ? 'bg-white dark:bg-gray-700 shadow-sm text-gray-900 dark:text-white'                                    : 'text-gray-500 dark:text-gray-400 hover:text-gray-700 dark:text-gray-300 dark:hover:text-gray-300'                            }`}                            title={t.label}                        >                            <Icon className="w-4 h-4" />                        </button>                    );                })}            </div>        );    }    return (        <div className="relative" ref={ref}>            <button                onClick={() => setOpen(!open)}                className="flex items-center gap-1.5 px-2.5 py-1.5 text-sm text-gray-600  
        $matches = [regex]::Matches(import { useState, useRef, useEffect } from 'react';import { Sun, Moon, Monitor, ChevronDown, Check } from 'lucide-react';import { useTheme } from '@/Contexts/ThemeContext';const themes = [    { code: 'light', label: 'Light', icon: Sun },    { code: 'dark', label: 'Dark', icon: Moon },    { code: 'system', label: 'System', icon: Monitor },];export default function ThemeSwitcher({ compact = false }) {    const { theme, switchTheme } = useTheme();    const [open, setOpen] = useState(false);    const ref = useRef(null);    const currentTheme = themes.find(t => t.code === theme) || themes[2];    useEffect(() => {        function handleClickOutside(e) {            if (ref.current && !ref.current.contains(e.target)) {                setOpen(false);            }        }        document.addEventListener('mousedown', handleClickOutside);        return () => document.removeEventListener('mousedown', handleClickOutside);    }, []);    if (compact) {        return (            <div className="flex items-center gap-1 p-1 bg-gray-100 dark:bg-gray-800 rounded-lg">                {themes.map((t) => {                    const Icon = t.icon;                    return (                        <button                            key={t.code}                            onClick={() => switchTheme(t.code)}                            className={`p-1.5 rounded-md transition-colors ${                                theme === t.code                                    ? 'bg-white dark:bg-gray-700 shadow-sm text-gray-900 dark:text-white'                                    : 'text-gray-500 dark:text-gray-400 hover:text-gray-700 dark:text-gray-300 dark:hover:text-gray-300'                            }`}                            title={t.label}                        >                            <Icon className="w-4 h-4" />                        </button>                    );                })}            </div>        );    }    return (        <div className="relative" ref={ref}>            <button                onClick={() => setOpen(!open)}                className="flex items-center gap-1.5 px-2.5 py-1.5 text-sm text-gray-600 dark:text-gray-400 dark:text-gray-500 hover:text-gray-900 dark:text-gray-100 dark:hover:text-gray-200 hover:bg-gray-100 dark:bg-gray-800 dark:hover:bg-gray-800 rounded-lg transition-colors"                title="Switch Theme"            >                {(() => {                    const Icon = currentTheme.icon;                    return <Icon className="w-4 h-4" />;                })()}                <span className="hidden sm:inline text-xs font-medium">{currentTheme.label}</span>                <ChevronDown className="w-3 h-3" />            </button>            {open && (                <div className="absolute right-0 mt-1 w-40 bg-white dark:bg-gray-800 rounded-lg shadow-lg border border-gray-200 dark:border-gray-800 dark:border-gray-700 py-1 z-50">                    {themes.map((t) => {                        const Icon = t.icon;                        return (                            <button                                key={t.code}                                onClick={() => {                                    switchTheme(t.code);                                    setOpen(false);                                }}                                className={`w-full flex items-center gap-2 px-3 py-2 text-sm hover:bg-gray-50 dark:bg-gray-950 dark:hover:bg-gray-700 transition-colors ${                                    theme === t.code ? 'text-blue-600 dark:text-blue-400 font-medium' : 'text-gray-700 dark:text-gray-300'                                }`}                            >                                <Icon className="w-4 h-4" />                                <span className="flex-1">{t.label}</span>                                {theme === t.code && <Check className="w-4 h-4 text-blue-600 dark:text-blue-400" />}                            </button>                        );                    })}                </div>            )}        </div>    );}.Value, 'dark:text-gray-\d+')
        if ($matches.Count -ge 2) { $matches[1].Value } else { import { useState, useRef, useEffect } from 'react';import { Sun, Moon, Monitor, ChevronDown, Check } from 'lucide-react';import { useTheme } from '@/Contexts/ThemeContext';const themes = [    { code: 'light', label: 'Light', icon: Sun },    { code: 'dark', label: 'Dark', icon: Moon },    { code: 'system', label: 'System', icon: Monitor },];export default function ThemeSwitcher({ compact = false }) {    const { theme, switchTheme } = useTheme();    const [open, setOpen] = useState(false);    const ref = useRef(null);    const currentTheme = themes.find(t => t.code === theme) || themes[2];    useEffect(() => {        function handleClickOutside(e) {            if (ref.current && !ref.current.contains(e.target)) {                setOpen(false);            }        }        document.addEventListener('mousedown', handleClickOutside);        return () => document.removeEventListener('mousedown', handleClickOutside);    }, []);    if (compact) {        return (            <div className="flex items-center gap-1 p-1 bg-gray-100 dark:bg-gray-800 rounded-lg">                {themes.map((t) => {                    const Icon = t.icon;                    return (                        <button                            key={t.code}                            onClick={() => switchTheme(t.code)}                            className={`p-1.5 rounded-md transition-colors ${                                theme === t.code                                    ? 'bg-white dark:bg-gray-700 shadow-sm text-gray-900 dark:text-white'                                    : 'text-gray-500 dark:text-gray-400 hover:text-gray-700 dark:text-gray-300 dark:hover:text-gray-300'                            }`}                            title={t.label}                        >                            <Icon className="w-4 h-4" />                        </button>                    );                })}            </div>        );    }    return (        <div className="relative" ref={ref}>            <button                onClick={() => setOpen(!open)}                className="flex items-center gap-1.5 px-2.5 py-1.5 text-sm text-gray-600 dark:text-gray-400 dark:text-gray-500 hover:text-gray-900 dark:text-gray-100 dark:hover:text-gray-200 hover:bg-gray-100 dark:bg-gray-800 dark:hover:bg-gray-800 rounded-lg transition-colors"                title="Switch Theme"            >                {(() => {                    const Icon = currentTheme.icon;                    return <Icon className="w-4 h-4" />;                })()}                <span className="hidden sm:inline text-xs font-medium">{currentTheme.label}</span>                <ChevronDown className="w-3 h-3" />            </button>            {open && (                <div className="absolute right-0 mt-1 w-40 bg-white dark:bg-gray-800 rounded-lg shadow-lg border border-gray-200 dark:border-gray-800 dark:border-gray-700 py-1 z-50">                    {themes.map((t) => {                        const Icon = t.icon;                        return (                            <button                                key={t.code}                                onClick={() => {                                    switchTheme(t.code);                                    setOpen(false);                                }}                                className={`w-full flex items-center gap-2 px-3 py-2 text-sm hover:bg-gray-50 dark:bg-gray-950 dark:hover:bg-gray-700 transition-colors ${                                    theme === t.code ? 'text-blue-600 dark:text-blue-400 font-medium' : 'text-gray-700 dark:text-gray-300'                                }`}                            >                                <Icon className="w-4 h-4" />                                <span className="flex-1">{t.label}</span>                                {theme === t.code && <Check className="w-4 h-4 text-blue-600 dark:text-blue-400" />}                            </button>                        );                    })}                </div>            )}        </div>    );}.Value }
     hover:text-gray-900 dark:text-gray-100 dark:hover:text-gray-200 hover:bg-gray-100 dark:bg-gray-800 dark:hover:bg-gray-800 rounded-lg transition-colors"                title="Switch Theme"            >                {(() => {                    const Icon = currentTheme.icon;                    return <Icon className="w-4 h-4" />;                })()}                <span className="hidden sm:inline text-xs font-medium">{currentTheme.label}</span>                <ChevronDown className="w-3 h-3" />            </button>            {open && (                <div className="absolute right-0 mt-1 w-40 bg-white dark:bg-gray-800 rounded-lg shadow-lg border border-gray-200 dark:border-gray-800 dark:border-gray-700 py-1 z-50">                    {themes.map((t) => {                        const Icon = t.icon;                        return (                            <button                                key={t.code}                                onClick={() => {                                    switchTheme(t.code);                                    setOpen(false);                                }}                                className={`w-full flex items-center gap-2 px-3 py-2 text-sm hover:bg-gray-50 dark:bg-gray-950 dark:hover:bg-gray-700 transition-colors ${                                    theme === t.code ? 'text-blue-600 dark:text-blue-400 font-medium' : 'text-gray-700 dark:text-gray-300'                                }`}                            >                                <Icon className="w-4 h-4" />                                <span className="flex-1">{t.label}</span>                                {theme === t.code && <Check className="w-4 h-4 text-blue-600 dark:text-blue-400" />}                            </button>                        );                    })}                </div>            )}        </div>    );}.Value }
     py-1 z-50">                    {themes.map((t) => {                        const Icon = t.icon;                        return (                            <button                                key={t.code}                                onClick={() => {                                    switchTheme(t.code);                                    setOpen(false);                                }}                                className={`w-full flex items-center gap-2 px-3 py-2 text-sm hover:bg-gray-50 dark:bg-gray-950 dark:hover:bg-gray-700 transition-colors ${                                    theme === t.code ? 'text-blue-600 dark:text-blue-400 font-medium' : 'text-gray-700 dark:text-gray-300'                                }`}                            >                                <Icon className="w-4 h-4" />                                <span className="flex-1">{t.label}</span>                                {theme === t.code && <Check className="w-4 h-4 text-blue-600 dark:text-blue-400" />}                            </button>                        );                    })}                </div>            )}        </div>    );}